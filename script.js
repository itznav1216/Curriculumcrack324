// CurricuForge – Safe, minimal JS
// No loops, no DOM duplication, no UI bugs

document.addEventListener("DOMContentLoaded", () => {
    console.log("CurricuForge UI loaded successfully");
});
